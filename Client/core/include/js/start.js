/* ЗАПУСКАЕМЫЕ СКРИПТЫ */

$(document).ready(() => {
    // создание структуры
    window.data = {
        computation_results: null,
        modified_results: null
    };

    // показ основной страницы
    showPageAirports();
});
